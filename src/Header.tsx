import React from 'react';

const Header = () => {
  return (
    <div style={{
      display: 'flex',
      marginBottom: 'auto',
      backgroundColor: 'blue',
      width: '100%',
      height: '5rem',
    }}>
      this is header
    </div>
  )
};

export default Header;
